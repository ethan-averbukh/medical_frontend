import strings from './../../strings.json';

const LogOutPage = () => {
    return (
        <div className="logout">
            <p>{strings.logout}</p>
        </div>
    )
}

export default LogOutPage;